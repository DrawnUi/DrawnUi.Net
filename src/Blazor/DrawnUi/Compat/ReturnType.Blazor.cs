namespace DrawnUi.Draw;

/// <summary>
/// Browser shim mirroring Microsoft.Maui.ReturnType so shared editor code compiles unchanged.
/// </summary>
public enum ReturnType
{
    Default,
    Done,
    Go,
    Next,
    Search,
    Send
}
