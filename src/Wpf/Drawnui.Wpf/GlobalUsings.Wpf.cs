// The WindowsDesktop SDK's implicit-usings set differs from the plain net9.0 one the Net and
// OpenTK heads compile against, so the namespaces shared code takes for granted are declared here.
global using System.IO;
global using System.Net.Http;
