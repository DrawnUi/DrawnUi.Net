using System.Windows;

namespace WpfPackageDemo;

/// <summary>Sample window hosting a drawn canvas declared entirely in WPF XAML.</summary>
public partial class MainWindow : Window
{
    /// <summary>Creates the window.</summary>
    public MainWindow()
    {
        InitializeComponent();
    }
}