---
title: Page Not Found
description: Redirecting to the DrawnUI for .NET home page.
---

<meta name="robots" content="noindex, nofollow">

<script>
(function () {
    var path = window.location.pathname;

    function looksLikeAssetPath(value) {
        if (!value || value === "/") {
            return false;
        }

        var pathname = value.split('?')[0].split('#')[0];
        var lastSegment = pathname.substring(pathname.lastIndexOf('/') + 1);
        return lastSegment.indexOf('.') !== -1;
    }

    if (window.location.protocol === "file:") {
        window.location.replace("./index.html");
        return;
    }

    var sampleRoots = [
        "/sandbox/",
        "/samples/wasm/blazor-sandbox/"
    ];

    for (var i = 0; i < sampleRoots.length; i++) {
        var sampleRoot = sampleRoots[i];

        if (path === sampleRoot.slice(0, -1) || path.indexOf(sampleRoot) === 0) {
            var suffix = path.slice(sampleRoot.length - 1);

            if (looksLikeAssetPath(suffix)) {
                continue;
            }

            var target = new URL(sampleRoot, window.location.origin);

            if (suffix && suffix !== "/") {
                target.searchParams.set('path', suffix);
            }

            window.location.replace(target.toString());
            return;
        }
    }

    window.location.replace("/");
})();
</script>

# Page Not Found

This page has moved or no longer exists.

Redirecting to the [DrawnUI for .NET home page](index.md).

If the redirect does not happen, use [Home](index.md).